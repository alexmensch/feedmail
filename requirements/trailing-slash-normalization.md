---
guid: 718F6B2C-024C-4E3B-8C65-A75C078EDDD9
date: 2026-03-08
feature: trailing-slash-normalization
---

#### Feature: Trailing slash normalization

Requests to both the API and Admin workers with trailing slashes (e.g. `/admin/login/`, `/api/subscribe/`) are not matched by route handlers that use exact string comparison, resulting in empty 404 responses or — in the case of bare `/admin` — no worker matching at all and a Cloudflare timeout. This feature normalizes URL paths early in both workers so that trailing slashes are handled consistently.

#### Requirements

| #   | Requirement                                                       | Description                                                                                                                                                                                                                                                                               | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                           | Edge Cases / Error Conditions                                                                                                                                                                                                                    |
| --- | ----------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Admin route pattern catches bare `/admin`                         | The Admin Worker's Cloudflare route pattern must match requests to `/admin` (no trailing slash) in addition to all `/admin/*` paths. Change the pattern from `YOUR_DOMAIN/admin/*` to `YOUR_DOMAIN/admin*`.                                                                               | A GET request to `https://{DOMAIN}/admin` reaches the Admin Worker and returns the dashboard page (with session) or redirects to login (without session). It must not time out or fall through to the origin.                                                                                                                                                 | Paths like `/adminfoo` or `/administrator` will also reach the Admin Worker — these must receive a 404 response from the worker's route handler.                                                                                                 |
| 2   | Admin Worker redirects trailing-slash GET requests                | When the Admin Worker receives a GET request whose path ends with a trailing slash (and is not bare `/`), it responds with a 301 redirect to the same URL with the trailing slash removed. The redirect must occur before rate limiting, session checks, or route matching.               | `GET /admin/login/` returns 301 with `Location: /admin/login`. `GET /admin/` returns 301 with `Location: /admin`. The response body is empty.                                                                                                                                                                                                                 | Multiple trailing slashes (e.g. `/admin/login//`) must also be stripped to the canonical form (`/admin/login`). Query strings must be preserved in the redirect target (e.g. `/admin/verify/?token=abc` redirects to `/admin/verify?token=abc`). |
| 3   | Admin Worker silently strips trailing slashes on non-GET requests | When the Admin Worker receives a non-GET request (e.g. POST) whose path ends with a trailing slash, the trailing slash is stripped internally before route matching. No redirect is issued.                                                                                               | `POST /admin/login/` is handled identically to `POST /admin/login` — the login form submission succeeds.                                                                                                                                                                                                                                                      | Multiple trailing slashes on POST must also be stripped.                                                                                                                                                                                         |
| 4   | API Worker silently strips trailing slashes                       | When the API Worker receives a request whose path ends with a trailing slash (and is not bare `/`), the trailing slash is stripped internally before any processing (method enforcement, rate limiting, authentication, route matching). No redirect is issued regardless of HTTP method. | `POST /api/subscribe/` is handled identically to `POST /api/subscribe`. `GET /api/verify/?token=abc` is handled identically to `GET /api/verify?token=abc`. A wrong method on a trailing-slash variant of a known route receives the same 10-second timeout as the non-trailing-slash version (e.g. `GET /api/subscribe/` behaves like `GET /api/subscribe`). | Multiple trailing slashes (e.g. `/api/subscribe//`) must be stripped. Bare `/` must not be affected.                                                                                                                                             |
| 5   | Admin Worker dashboard route simplified                           | After normalization is in place, the Admin Worker's dashboard route check matches only `/admin` (without needing to also check `/admin/`).                                                                                                                                                | `GET /admin` (after normalization) returns the dashboard page or redirects to login. The route handler does not contain a separate check for `/admin/`.                                                                                                                                                                                                       | N/A — this is a simplification enabled by requirements 2 and 3.                                                                                                                                                                                  |
| 6   | Version bump                                                      | The package version is bumped as a patch release to reflect the bug fix.                                                                                                                                                                                                                  | `package.json` version is incremented from `2.2.0` to `2.2.1`.                                                                                                                                                                                                                                                                                                | N/A                                                                                                                                                                                                                                              |

#### Out of scope

- **API Worker route pattern change:** The API Worker keeps its existing `YOUR_DOMAIN/api/*` route pattern. Bare `/api` is not a meaningful endpoint and does not need to be caught.
- **Redirect behaviour for API Worker:** The API Worker silently strips trailing slashes. No 301/308 redirects — API clients do not benefit from URL bar updates.
- **Canonical URL enforcement beyond trailing slashes:** Other URL normalization (e.g. lowercasing paths, removing double slashes mid-path) is not in scope.
- **Migration script for `wrangler.admin.prod.toml`:** The route pattern change applies to the template `wrangler.admin.toml`. Existing deployers will need to manually update their `wrangler.admin.prod.toml` — a migration script is deferred.
