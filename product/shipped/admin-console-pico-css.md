---
guid: BF3EAFC3-7641-4004-A3CD-2C050677DDBC
date: 2026-03-13
feature: admin-console-pico-css
depends-on:
  - 5A963535-83B6-4BA9-AB36-0A8C4F29E7BC
---

## Feature: Admin Console Pico CSS

Adds visual styling to the entire feedmail HTML interface — admin console pages, auth pages, and all public-facing HTML pages (subscription confirmation, verification, unsubscribe, error) — using Pico CSS as the design foundation. The functional console (admin-console-functional) provides plain HTML forms and pages with no visual design. Pico CSS is a semantic-first minimal CSS framework that styles standard HTML elements with professional defaults, built-in dark mode, and fluid responsive design, requiring no build step. All HTML-rendering templates adopt Pico's semantic markup conventions and default styling, with a minimal override stylesheet for feedmail-specific components. The result is a polished, responsive experience across every user-facing and admin-facing page that establishes the baseline visual trust needed for people evaluating feedmail as a serious tool.

**Implementation note:** This feature supersedes the admin-console-styled feature (guid `D04F43C0-AF8F-4CDA-B9ED-4E9C1D3ACA1B`, PR #56). Implementation must begin from the codebase state _before_ PR #56 was merged — that PR's CUBE CSS styling, Every Layout primitives, template markup changes, and static assets (`styles.css` content, layout partials) are fully replaced by this feature. Any non-styling changes from subsequent commits should be preserved.

## Requirements

| #   | Requirement                                          | Description                                                                                                                                                                                                                                                                                                                                                                                                                                               | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                        | Edge Cases / Error Conditions                                                                                                                                                                                                                               |
| --- | ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Add Pico CSS as a static asset                       | Pico CSS (default variant) is committed to the `assets/admin/` directory alongside `htmx.min.js` and served via Cloudflare Workers static assets. All admin console and public-facing HTML pages load Pico CSS before any override stylesheet.                                                                                                                                                                                                            | `pico.min.css` exists in `assets/admin/`. The shared `<head>` partial references Pico CSS before `styles.css`. Pages render with Pico's default styling when `styles.css` is empty.                                                                                                                                                                        | Pico CSS file must be the production minified build. If Pico is loaded after the override stylesheet, specificity conflicts will produce incorrect styling.                                                                                                 |
| 2   | Semantic markup — layout and navigation              | All admin console page templates use Pico's semantic HTML conventions for layout and navigation. Navigation uses `<nav>` with `<ul>` elements. The authenticated page layout provides navigation and a content area using Pico-idiomatic structure. The unauthenticated (auth) page layout uses a centered layout appropriate for single-form pages.                                                                                                      | Every admin console page renders within a Pico-styled layout. Navigation links are functional and visually indicate the current page. The layout is responsive — navigation and content reflow appropriately on narrow viewports. Auth pages (login, magic link sent, auth error) are centered and visually distinct from authenticated pages.             | Pages with no navigation (auth pages) must not render an empty nav element. Active page indication must use a Pico-supported pattern (e.g., `aria-current="page"`).                                                                                         |
| 3   | Semantic markup — content elements                   | All card-like containers, tables, forms, buttons, feedback messages, and other content elements across admin console and public-facing HTML pages use Pico's semantic conventions (e.g., `<article>` for card-like groupings, native `<table>` styling, native form element styling). No custom CSS classes are needed for these generic components.                                                                                                      | Tables, forms, buttons, and cards render with Pico's default styling. Buttons have visually distinct primary, secondary, and destructive variants using Pico's conventions. Feedback/alert messages for success and error states are visually distinct. All public-facing pages (verify, unsubscribe, error) render with Pico styling.                     | Forms with multiple sections (e.g., the channel form with inline feeds) must remain visually scannable — sections need clear separation. Buttons inside tight layouts (e.g., inline confirm dialogs, feed row actions) must not overflow or wrap awkwardly. |
| 4   | Override stylesheet for feedmail-specific components | `styles.css` contains only styles for components that Pico does not cover: feed row management (add/remove feed rows in channel form), passkey bootstrap prompt, inline delete confirmation dialogs, input suffix pattern (`@domain`), HTMX loading indicators, session expired fragment, and any other feedmail-specific UI patterns. No generic resets, typography, base element styles, or layout primitives are included — Pico handles all of these. | Every rule in `styles.css` targets a feedmail-specific component that has no Pico equivalent. All feedmail-specific components render correctly with their override styles. Override styles use Pico's custom properties (e.g., `--pico-primary`, `--pico-color`) for colors and spacing where possible, ensuring visual consistency with Pico's defaults. | Override styles must not conflict with Pico's specificity. Hard-coded color values in overrides will break dark mode — all colors must reference Pico custom properties or use dark mode media queries.                                                     |
| 5   | Dark mode                                            | Dark mode works automatically via `prefers-color-scheme: media` (Pico's default behavior). All pages — admin console, auth, and public-facing — render correctly in both light and dark modes. Feedmail-specific override styles in `styles.css` respect the active color scheme.                                                                                                                                                                         | Light and dark modes produce legible, visually coherent results on every page. No hard-coded colors in override styles that break in the opposite mode. Pico's automatic theme switching is not overridden or disabled.                                                                                                                                    | Feedmail-specific components (passkey prompt, inline confirm, feed rows) are the most likely to have color issues in dark mode — these must be explicitly verified in both modes.                                                                           |
| 6   | HTMX interactions                                    | All HTMX-powered interactions function correctly: send trigger with feedback, subscriber table filtering, channel form result swap, delete confirmation dialogs, passkey list updates, and session expired fragment injection. `hx-target`, `hx-swap`, and `hx-trigger` attributes target the correct elements in the new markup structure. Fragment templates produce HTML that integrates correctly with the page structure.                            | Every HTMX interaction works: form submissions swap content into the correct target, delete confirmations appear inline, subscriber table updates without full page reload, loading indicators display during requests. No JavaScript errors in the browser console during HTMX operations.                                                                | Fragment templates that are registered as both standalone templates and partials (e.g., `admin-subscriber-table`) must render correctly in both contexts. Session expired fragments must be injectable into any HTMX target container.                      |
| 7   | Accessibility                                        | All pages maintain accessibility: focus-visible outlines, reduced motion support, `aria-live` regions for dynamic content, form labels, and semantic heading hierarchy. Pico provides most of these by default; any that require explicit implementation are included in the override stylesheet or templates.                                                                                                                                            | Screen reader navigation produces a logical reading order on every page. Focus indicators are visible on all interactive elements. `prefers-reduced-motion` disables animations. All form inputs have associated labels.                                                                                                                                   | HTMX content swaps must announce to screen readers via `aria-live` regions. Any accessibility features not provided by Pico's defaults must be explicitly implemented.                                                                                      |

## Out of scope

- **Functional changes** — No new pages, routes, API changes, or behavioral modifications. The admin console does exactly what it does today.
- **Custom theming or branding** — No feedmail logo, custom color palette, or brand identity. Pico's defaults are the look.
- **Email templates** — `newsletter.hbs`, `newsletter.txt.hbs`, `verification-email.hbs`, `partials/email-footer.hbs`, and `admin-magic-link.hbs` are HTML emails with inline styles, not part of the admin console CSS. These are untouched.
- **JavaScript refactoring** — WebAuthn inline JS, the HTMX library itself, and inline scripts are not being refactored. Only `hx-*` attributes change if container elements change.
- **Pico customization or theming** — No custom Pico theme, no overriding Pico's color palette or typography scale. The default Pico look is accepted as-is.
- **Build pipeline** — No CSS preprocessor, bundler, or build step is introduced. Pico is committed as a static file, same as `htmx.min.js`.

## Technical Specification

### Summary

Replace CUBE CSS with Pico CSS across all feedmail HTML pages. Pico CSS v2 (default variant) is committed as a static asset (`assets/admin/pico.min.css`). All admin console templates adopt Pico's semantic markup conventions. The CUBE CSS stylesheet is completely replaced with a minimal override stylesheet for feedmail-specific components only. Implementation approach: rewrite from current master, replacing all CUBE CSS patterns with Pico equivalents.

### Requirements

| #   | Requirement                                                     | Description                                                                                                                                                                                                                                                                                                                                                                                                  | Acceptance Criteria                                                                                                                                                                                                                                                                                                                                                                                                     | Edge Cases / Error Conditions                                                                                                                                                                                                                                                                                                                                                              |
| --- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Add Pico CSS static asset                                       | Commit `pico.min.css` (production minified, default variant) to `assets/admin/`. Served via Workers static assets alongside `htmx.min.js`.                                                                                                                                                                                                                                                                   | File `assets/admin/pico.min.css` exists and is the minified production build of Pico CSS v2 (default variant). File size is reasonable for the current Pico release (~80KB minified).                                                                                                                                                                                                                                   | Must not be a development/unminified build. Must be the default variant (not classless, conditional, or fluid), since the admin console uses `class="container"` and Pico's grid system.                                                                                                                                                                                                   |
| 2   | Update admin-head partial to reference Pico CSS                 | The `admin-head.hbs` partial loads `pico.min.css` before `styles.css`. Both referenced as absolute paths from `/admin/`.                                                                                                                                                                                                                                                                                     | `admin-head.hbs` contains two `<link rel="stylesheet">` elements: first `/admin/pico.min.css`, then `/admin/styles.css`. Removing `styles.css` reference leaves pages fully styled by Pico defaults.                                                                                                                                                                                                                   | Load order matters: Pico must come first so `styles.css` overrides take effect. Both files are served by the Admin Worker's static assets configuration.                                                                                                                                                                                                                                   |
| 3   | Rewrite admin-layout partial for Pico semantics                 | Replace CUBE CSS sidebar layout (`div.app-shell.sidebar-layout`) with Pico-idiomatic structure: `<body>` contains a `<nav>` and `<main class="container">`. Nav is a horizontal top bar. Content area uses `{{> content}}` partial block. HTMX script tag preserved.                                                                                                                                         | Authenticated admin pages render with a top navigation bar and content area below. `<main>` has `class="container"`. HTMX script tag (`/admin/htmx.min.js`) included before `</body>`. Template uses `{{> content}}` inline partial mechanism.                                                                                                                                                                          | Must not use CUBE CSS classes (`sidebar-layout`, `app-shell`, `main-content`, `stack`). `<nav>` is a direct child of `<body>`, before `<main>`.                                                                                                                                                                                                                                           |
| 4   | Rewrite admin-nav partial for Pico semantics                    | Replace CUBE CSS nav markup (`nav.nav-sidebar` with BEM classes) with Pico-idiomatic nav: `<nav>` containing `<ul>` with `<li>` items. Brand link and logout positioned using Pico's nav list conventions. Active page uses `aria-current="page"`.                                                                                                                                                           | Navigation renders as horizontal bar: brand ("feedmail"), Dashboard, Channels, Subscribers, Settings, Logout. Active page visually distinguished via `aria-current="page"`. Logout visually separated (right-aligned group). Multiple `<ul>` elements inside `<nav>` (first for brand/left items, last for right items).                                                                                                | No CUBE CSS classes or `data-state="active"`. `aria-current="page"` replaces `data-state="active"`.                                                                                                                                                                                                                                                                                       |
| 5   | Rewrite admin-auth-layout partial for Pico semantics            | Replace CUBE CSS auth layout (`div.auth-layout` + `div.auth-card`) with Pico-idiomatic centered layout: `<main class="container">` with `<article>` for the centered card. No nav element. No HTMX script (auth pages do not use HTMX, except login page which has inline WebAuthn JS).                                                                                                                    | Auth pages (login, login-sent, auth-error) render centered with card-like container. No navigation bar visible. Brand text "feedmail" displayed. `{{> content}}` inline partial block preserved.                                                                                                                                                                                                                        | No CUBE CSS classes (`auth-layout`, `auth-card`, `auth-card__brand`). Layout must NOT include HTMX script tag.                                                                                                                                                                                                                                                                            |
| 6   | Rewrite admin-dashboard template for Pico semantics             | Replace CUBE CSS markup: channel stats in `<article>` elements (not `div.card`), tables use native `<table>`, feedback uses custom classes, send form and HTMX attributes preserved.                                                                                                                                                                                                                         | Dashboard renders with: passkey prompt (if shown), send button with HTMX loading indicator, channel stat cards as `<article>` elements with `<h2>` and stats table. Empty state links to create channel. Feedback messages visually distinct. All HTMX attributes preserved and functional.                                                                                                                             | No CUBE CSS classes. Pico has no built-in alert/feedback component, so override stylesheet provides feedback styling via `.feedback`, `.feedback--success`, `.feedback--error`.                                                                                                                                                                                                             |
| 7   | Rewrite admin-channels template for Pico semantics              | Replace CUBE CSS markup: page header uses native elements, table uses native `<table>`, "Create channel" button uses Pico conventions.                                                                                                                                                                                                                                                                       | Channel list page renders with heading, "Create channel" link/button, channel table with ID/Site Name/Site URL columns. Empty state message shown when no channels. `role="button"` on links for Pico button styling.                                                                                                                                                                                                   | No CUBE CSS classes (`page-header`, `btn`, `btn-primary`, `table`, `text-muted`).                                                                                                                                                                                                                                                                                                         |
| 8   | Rewrite admin-channel-form template and form-body partial       | Replace CUBE CSS form markup. Forms use native `<label>` + `<input>` pairs. Feed rows use feedmail-specific structure styled by override sheet. Input-suffix pattern (`@domain`) remains feedmail-specific. Inline JS (`addFeedRow`, `removeFeedRow`, `showError`, `showHelper`, validation) references new CSS classes/structure.                                                                            | Channel create/edit form renders with all fields properly labeled/styled. Feed rows visually grouped and editable. Add/remove feed buttons work (JS and noscript). Channel ID and from-user inline validation work. CORS auto-populate works. HTMX form submission works on edit (targets `#form-result`). "Actions" section renders on edit.                                                                            | Inline JS in `admin-channel-form.hbs` builds HTML strings for new feed rows — must match markup in `admin-channel-form-body.hbs`. `showError`/`showHelper` class names must match override stylesheet. `admin-channel-form-body.hbs` partial shared between full-page template and `admin-channel-form-result.hbs` fragment.                                                               |
| 9   | Rewrite admin-channel-form-result fragment                      | Update HTMX fragment template to match new markup conventions. Swapped into `#form-result` on channel edit page.                                                                                                                                                                                                                                                                                             | Fragment renders feedback messages and channel form body partial using new conventions. No document-level elements. HTMX swap works correctly.                                                                                                                                                                                                                                                                          | `admin-channel-form-body` (used within it) must be registered as a partial.                                                                                                                                                                                                                                                                                                                |
| 10  | Rewrite admin-subscribers + subscriber-table fragment           | Replace CUBE CSS form/table/feedback markup. Filter form uses native Pico form elements. Table uses native `<table>`. Subscriber-table fragment registered as both template and partial.                                                                                                                                                                                                                     | Subscriber page renders with channel/status filter dropdowns, filter button, subscriber table. HTMX filtering works (swaps `#subscriber-table`). Table shows Email, Channel, Status, Subscribed columns. Empty state and error messages display correctly.                                                                                                                                                              | `admin-subscriber-table.hbs` must continue as both template (HTMX fragment) and partial (inclusion in `admin-subscribers.hbs`).                                                                                                                                                                                                                                                           |
| 11  | Rewrite admin-settings template for Pico semantics              | Replace CUBE CSS markup for passkey management page. Passkey table, rename forms, register section use Pico conventions.                                                                                                                                                                                                                                                                                     | Settings page renders with passkey table (Name, Registered, Actions), rename form per passkey, delete buttons with HTMX confirmation, "Register New Passkey" section. All HTMX interactions work.                                                                                                                                                                                                                      | Button variants: primary for register, danger for delete, small for inline actions — need Pico-compatible implementations.                                                                                                                                                                                                                                                                 |
| 12  | Rewrite admin-passkey-list fragment                             | Update passkey list HTMX fragment. Swapped into `#passkey-list`.                                                                                                                                                                                                                                                                                                                                             | Fragment renders passkey table with feedback messages. No document-level elements. Integrates correctly when swapped into `#passkey-list`.                                                                                                                                                                                                                                                                              | Must match table structure in `admin-settings.hbs` exactly.                                                                                                                                                                                                                                                                                                                                |
| 13  | Rewrite admin-login template for Pico semantics                 | Replace CUBE CSS form markup. Login form uses native Pico form elements within auth layout. Passkey button and magic link form visually distinct.                                                                                                                                                                                                                                                            | Login page renders centered with passkey button (if passkeys exist), magic link email form, error display. WebAuthn inline JS works. Hidden `#passkey-error` div present.                                                                                                                                                                                                                                               | `#passkey-error` div must be styled appropriately when shown via JS.                                                                                                                                                                                                                                                                                                                       |
| 14  | Rewrite admin-login-sent and admin-auth-error templates         | Replace CUBE CSS markup in auth page templates.                                                                                                                                                                                                                                                                                                                                                              | "Check your email" page renders centered with informational text. Auth error page renders centered with error message and login link. Both use auth layout.                                                                                                                                                                                                                                                             | No CUBE CSS classes.                                                                                                                                                                                                                                                                                                                                                                       |
| 15  | Rewrite admin-send-feedback fragment                            | Update send trigger feedback fragment.                                                                                                                                                                                                                                                                                                                                                                       | Success and error feedback renders with visually distinct styling. No document-level elements. Swaps correctly into `#send-feedback`.                                                                                                                                                                                                                                                                                   | Feedback styling must come from override stylesheet (`.feedback`, `.feedback--success`, `.feedback--error`).                                                                                                                                                                                                                                                                               |
| 16  | Rewrite admin-delete-confirm fragment                           | Update inline delete confirmation dialog. Buttons use Pico conventions for primary/danger/secondary variants.                                                                                                                                                                                                                                                                                                | Delete confirmation renders with warning message, Confirm button (destructive), Cancel button. HTMX attributes preserved. Integrates into `#channel-actions` or `#passkey-delete-{id}`.                                                                                                                                                                                                                                | `cancelHtml` parameter in route handlers builds HTML strings with button classes — must match new markup.                                                                                                                                                                                                                                                                                  |
| 17  | Rewrite admin-session-expired fragment                          | Update session expired fragment.                                                                                                                                                                                                                                                                                                                                                                             | Session expired message renders with warning styling and login link. Meta refresh tag preserved. No document-level elements. Injectable into any HTMX target.                                                                                                                                                                                                                                                           | Must be styled as visually distinct warning/error via `.session-expired` class.                                                                                                                                                                                                                                                                                                            |
| 18  | Rewrite public-facing pages for Pico styling                    | Replace inline `<style>` blocks in `verify-page.hbs`, `unsubscribe-page.hbs`, `error-page.hbs` with Pico CSS loaded from CDN (`https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css`). Use Pico semantic structure: centered `<main class="container">` with `<article>`.                                                                                                                         | All three public pages render with Pico styling. Centered, responsive, legible in both light/dark modes. Site name, heading, message, back link visible and styled. No inline `<style>` block with custom CSS.                                                                                                                                                                                                          | API Worker has no `[assets]` block — cannot serve static files. CDN link introduces external dependency (acceptable for infrequent, non-critical public pages).                                                                                                                                                                                                                            |
| 19  | Replace styles.css with Pico override stylesheet                | Completely replace 804-line CUBE CSS stylesheet with minimal override stylesheet containing only feedmail-specific component styles.                                                                                                                                                                                                                                                                         | `styles.css` contains only rules for: feed rows (`.feed-rows`, `.feed-row`, `.feed-row-actions`), input suffix (`.input-suffix`), HTMX indicators (`.htmx-indicator`, `.loading-spinner`), passkey prompt (`.passkey-prompt`), inline confirm (`.inline-confirm`, `.inline-confirm__actions`), session expired (`.session-expired`), feedback (`.feedback`, `.feedback--success`, `.feedback--error`), required indicator (`.required::after`), helper text (`.helper-text`, `.field-error`), `.btn-danger`, `.btn-small`. No generic resets, typography, or layout primitives. | All colors use Pico custom properties (`--pico-primary`, `--pico-color`, `--pico-background-color`, `--pico-muted-color`, `--pico-del-color`, `--pico-ins-color`, `--pico-muted-border-color`, `--pico-card-background-color`, `--pico-form-element-*`). No hard-coded hex colors. `prefers-reduced-motion` for loading spinner. Low-specificity class selectors to avoid fighting Pico. |
| 20  | Remove dead CUBE CSS classes from templates                     | After all rewrites, verify no template references any CUBE CSS class.                                                                                                                                                                                                                                                                                                                                        | No `.hbs` file contains any CUBE CSS classes: `sidebar-layout`, `app-shell`, `nav-sidebar`, `card` (as class), `form-field`, `btn`, `btn-primary`, `btn-danger`, `btn-small`, `btn-ghost`, `cluster`, `stack`, `center`, `cover`, `flow`, `text-muted`, `font-size--1`, `font-size-1`, `feedback` (without `--`), `auth-layout`, `auth-card`, `channel-actions`, `page-header`, `visually-hidden`.                     | Also verify `data-variant`, `data-state`, `data-gap` attributes are no longer used.                                                                                                                                                                                                                                                                                                       |
| 21  | Update cancelHtml strings in route handlers                     | `handleChannelDeleteConfirm` and `handlePasskeyDeleteConfirm` in `channels.js` and `passkeys.js` build HTML strings with CUBE CSS button classes. Update to new Pico-compatible markup.                                                                                                                                                                                                                     | `cancelHtml` strings use new button markup conventions. Delete confirmation cancel/re-trigger buttons render correctly.                                                                                                                                                                                                                                                                                                 | String literals in JavaScript, not templates. Must stay in sync with delete confirm template.                                                                                                                                                                                                                                                                                              |
| 22  | Update addFeedRow HTML string in admin-channel-form             | `addFeedRow()` JS function builds HTML string with CUBE CSS classes. Update to match new feed row markup.                                                                                                                                                                                                                                                                                                    | Dynamically added feed rows match server-rendered feed rows in `admin-channel-form-body.hbs`. Remove button works. Feed inputs properly labeled/styled.                                                                                                                                                                                                                                                                | JS-built HTML and Handlebars template markup must be identical in structure and class usage.                                                                                                                                                                                                                                                                                                |
| 23  | Dark mode correctness                                           | All override styles use Pico custom properties for colors. No `@media (prefers-color-scheme: dark)` needed if all colors derive from Pico properties.                                                                                                                                                                                                                                                        | Every page renders correctly in both light and dark mode. Feedmail-specific components have correct colors in both modes. No hard-coded color values in `styles.css`.                                                                                                                                                                                                                                                   | Must not include `[data-theme]` or any mechanism overriding Pico's automatic switching.                                                                                                                                                                                                                                                                                                   |
| 24  | HTMX interaction preservation                                   | All HTMX interactions function after markup changes: dashboard send (`#send-feedback`), subscriber filter (`#subscriber-table`), channel form save (`#form-result`), channel delete confirm (`#channel-actions`), passkey rename (`#passkey-list`), passkey delete confirm (`#passkey-delete-{id}`), session expired injection.                                                                                | Each interaction triggers correctly, sends to correct endpoint, swaps into correct target, renders correctly. Loading indicators appear during requests.                                                                                                                                                                                                                                                                | Target element IDs must be present with exact same IDs. Fragment content must integrate correctly if container element type or nesting changes.                                                                                                                                                                                                                                            |
| 25  | Accessibility preservation                                      | Focus-visible outlines (Pico default). `aria-current="page"` on active nav. `aria-live` on HTMX swap targets. `role="alert"` on feedback. All form inputs have `<label>`. Semantic heading hierarchy. `prefers-reduced-motion` for loading spinner.                                                                                                                                                          | Keyboard navigation reaches all interactive elements. Focus indicators visible. Screen reader announces dynamic content. Every input has a label. No heading level skipped.                                                                                                                                                                                                                                             | HTMX swap targets should have `aria-live="polite"`. Loading spinner animation disabled under `prefers-reduced-motion: reduce`.                                                                                                                                                                                                                                                            |
| 26  | Remove dead CUBE CSS code from styles.css                       | Previous CUBE CSS tokens, reset, compositions, utilities, blocks, exceptions entirely removed. No residual CUBE CSS custom properties.                                                                                                                                                                                                                                                                       | `styles.css` contains zero CUBE CSS custom properties (`--color-*`, `--space-*`, `--step-*`, `--measure`, `--radius`, `--transition-speed`), zero CUBE CSS class definitions, zero CUBE CSS data-attribute selectors. File size significantly smaller than 804 lines.                                                                                                                                                   | If any template still references a CUBE CSS class (caught by #20), styling silently breaks. #20 and #26 together provide the safety net.                                                                                                                                                                                                                                                  |

### Implementation Notes

#### Pico CSS static asset (Req 1)
- **New file:** `assets/admin/pico.min.css` — download production minified build of Pico CSS v2 (default variant)
- No changes to `wrangler.admin.toml` — `[assets]` config already serves everything in `assets/`
- No changes to `eslint.config.js` — `assets/` already in ignores list

#### Layout and navigation partials (Reqs 2–5)
- **Files:** `admin-head.hbs`, `admin-layout.hbs`, `admin-nav.hbs`, `admin-auth-layout.hbs`
- `{{> content}}` inline partial mechanism (via `{{#*inline "content"}}`) must be preserved
- `{{> admin-head}}` partial inclusion preserved in both layouts
- `activePage` parameter flows from page templates through layout to nav partial
- Pico nav pattern: multiple `<ul>` elements inside `<nav>` (first for brand/left, last for right items like logout)

#### Page and fragment template rewrites (Reqs 6–17)
- Key pattern replacements:
  - `class="card"` on `<div>` → `<article>` element
  - `class="table"` on `<table>` → bare `<table>`
  - `class="feedback" data-variant="..."` → `.feedback.feedback--error` / `.feedback--success`
  - `class="form-field"` wrapper → remove; Pico styles `<label>` + `<input>` pairs natively
  - `class="stack"` / `class="cluster"` → remove; use Pico's `role="group"` for button groups
  - `class="btn btn-primary"` → default `<button>` (already primary in Pico)
  - Secondary buttons: `class="secondary"`. Contrast: `class="contrast"`
  - `class="text-muted"` → `<small>` element
  - `role="button"` on `<a>` for link-as-button styling

#### Public-facing pages (Req 18)
- **Files:** `verify-page.hbs`, `unsubscribe-page.hbs`, `error-page.hbs`
- CDN link: `https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css`
- Replace inline `<style>` blocks with Pico semantic structure

#### Override stylesheet (Req 19)
- **File:** `assets/admin/styles.css` — complete replacement
- CSS classes to define: `.feed-rows`, `.feed-row`, `.feed-row-actions`, `.remove-feed-btn`, `.input-suffix`, `.htmx-indicator`, `.loading-spinner`, `.passkey-prompt`, `.inline-confirm`, `.inline-confirm__actions`, `.session-expired`, `.feedback`, `.feedback--success`, `.feedback--error`, `.required`, `.helper-text`, `.field-error`, `.btn-danger`, `.btn-small`
- All colors via Pico custom properties
- `prefers-reduced-motion` for loading spinner animation

#### Route handler updates (Reqs 21–22)
- **Files:** `src/admin/routes/channels.js`, `src/admin/routes/passkeys.js` — cancelHtml strings
- **File:** `src/templates/admin-channel-form.hbs` — addFeedRow JS function HTML string

### No changes needed to
- `scripts/precompile-templates.mjs`, `src/shared/lib/templates.js` (same template/partial names)
- `wrangler.admin.toml` (already has `[assets]` config)
- All route handler logic (only cancelHtml strings change)
- Email templates (out of scope)
- `partials/webauthn-helpers.hbs` (pure JavaScript)
- `src/admin/lib/htmx.js`, `src/admin/lib/session.js`

## Out-of-spec changes

Changes made during code review that were not in the original specification:

| #   | Change                                            | Description                                                                                                                                                                                                      |
| --- | ------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Extract public-page-layout partial                | Created `partials/public-page-layout.hbs` shared partial for verify, unsubscribe, and error pages. Eliminates duplication of CDN URL, `<head>`, `<main>`, `<article>`, and `<footer>` structure across 3 files.   |
| 2   | Pin CDN Pico CSS version                          | Changed CDN URL from `@2` (floating) to `@2.1.1` (pinned) to match the vendored `pico.min.css` version and prevent silent version drift.                                                                        |
| 3   | Deduplicate helper-text/field-error CSS           | Combined shared properties of `.helper-text` and `.field-error` into a grouped selector, with only `color` differing in individual rules.                                                                        |
| 4   | Extract cancelHtml to local constants             | In `channels.js` and `passkeys.js`, extracted duplicated `cancelHtml` button strings to local `const` variables within each handler function.
