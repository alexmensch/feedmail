allowBuilds:
  esbuild: true
  sharp: true
  workerd: true

# vitest's toolchain (@vitest/mocker) pulls vite, which only resolves to
# 7.3.1 here; >=7.3.2 patches the dev-server file-read / fs.deny-bypass
# advisories. Drop the override once vitest's transitive vite floor moves.
overrides:
  vite: "^7.3.2"
