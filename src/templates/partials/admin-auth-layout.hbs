<!DOCTYPE html>
<html lang="en">
<head>
  {{> admin-head}}
  <title>{{pageTitle}} — feedmail admin</title>
</head>
<body>
  <div class="auth-layout">
    <div class="auth-card">
      <div class="auth-card__brand">feedmail</div>
      <div class="stack" data-gap="m">
        {{> content}}
      </div>
    </div>
  </div>
</body>
</html>